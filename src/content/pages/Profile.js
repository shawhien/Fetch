import React from 'react';
import { Route } from 'react-router-dom';
import ProfileInfo from '../json/JSONContent.json';

export default function Profile(props) {

    return (
        <div>
            <h1>ProfileInfo[0].humanInfo.name</h1>
        </div>
    )
}